name: BullsEye
description: My First Flutter Project

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
      sdk: flutter

dev_dependencies:
    flutter_test:
            sdk: flutter

pedantic: '>=1.4.0 <3.0.0'

flutter:
  uses-material-design: true
